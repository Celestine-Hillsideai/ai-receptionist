# /temp/resources/

Temporary input or reference material the Agent needs mid-task — e.g. a file the user dropped in for a one-off task, downloaded reference data, sample payloads. Treat everything else in this folder as disposable; ignored by git except for this README.
